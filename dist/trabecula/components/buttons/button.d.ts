import { ReactNode } from "react";
import { ButtonProps as MuiButtonProps } from "@mui/material";
import { IconName, IconProps, TooltipProps } from "trabecula/components";
import { BorderRadiuses, CSS, Margins, Padding } from "trabecula/utils/client";
export interface ButtonProps extends Omit<MuiButtonProps, "color" | "endIcon" | "fullWidth" | "startIcon" | "type" | "variant"> {
    borderRadiuses?: BorderRadiuses;
    boxShadow?: CSS["boxShadow"];
    color?: string;
    colorOnHover?: string;
    endNode?: ReactNode;
    fontSize?: CSS["fontSize"];
    fontWeight?: CSS["fontWeight"];
    icon?: IconName;
    iconProps?: Partial<IconProps>;
    iconRight?: IconName;
    iconSize?: string | number;
    justify?: CSS["justifyContent"];
    loading?: boolean;
    margins?: Margins;
    outlined?: boolean;
    outlineFill?: string;
    padding?: Padding;
    startNode?: ReactNode;
    text?: string;
    textColor?: string;
    textClassName?: string;
    textTransform?: CSS["textTransform"];
    tooltip?: TooltipProps["title"];
    tooltipProps?: Partial<TooltipProps>;
    type?: "button" | "link";
    width?: CSS["width"];
}
export declare const Button: ({ borderRadiuses, boxShadow, children, className, color, colorOnHover, endNode, fontSize, fontWeight, href, icon, iconProps, iconRight, iconSize, justify, loading, margins, onClick, outlined, outlineFill, padding, size, startNode, text, textColor, textTransform, tooltip, tooltipProps, type, width, ...props }: ButtonProps) => import("react").JSX.Element;
