import { ComponentProps } from "react";
import { Autocomplete } from "@mui/material";
import { HeaderWrapperProps, InputProps } from "trabecula/components";
import { TagOption } from "trabecula/store";
import { CSS, Margins } from "trabecula/utils/client";
export type TagInputProps = Omit<ComponentProps<typeof Autocomplete>, "defaultValue" | "fullWidth" | "label" | "renderInput" | "onChange" | "onSelect" | "options"> & {
    autoFocus?: boolean;
    center?: boolean;
    excludedIds?: string[];
    hasCreate?: boolean;
    hasDelete?: boolean;
    hasDeleteAll?: boolean;
    hasEditor?: boolean;
    hasHelper?: boolean;
    hasList?: boolean;
    hasSearchMenu?: boolean;
    header?: HeaderWrapperProps["header"];
    headerProps?: HeaderWrapperProps["headerProps"];
    includedIds?: string[];
    inputProps?: InputProps;
    margins?: Margins;
    maxTags?: number;
    onChange?: (val: TagOption[]) => void;
    onSelect?: (val: TagOption) => void;
    onTagClick?: (tagOpt: TagOption) => void;
    single?: boolean;
    value: TagOption[];
    width?: CSS["width"];
};
export declare const TagInput: any;
